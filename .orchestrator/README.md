# .orchestrator

这里只放运行时状态，不放源码。

目录约定：

- `runs/<run-id>/state.json`
- `runs/<run-id>/events.jsonl`
- `runs/<run-id>/artifacts/`
- `runs/<run-id>/workers/`

`runs/` 已加入 `.gitignore`，避免把实验运行痕迹提交进仓库。

